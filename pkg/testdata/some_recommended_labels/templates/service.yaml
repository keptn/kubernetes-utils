apiVersion: v1
kind: Service
metadata:
  name: {{ include "some_recommended_labels.fullname" . }}
  labels:
    {{- include "some_recommended_labels.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "some_recommended_labels.selectorLabels" . | nindent 4 }}
